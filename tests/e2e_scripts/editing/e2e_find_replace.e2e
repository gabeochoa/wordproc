# Test: Find dialog opens (Ctrl+F)
# Validates find dialog can be triggered via keyboard shortcut

# Type some text
type "Hello World"
wait 2

# Open find dialog (Ctrl+F)
key CTRL+F
wait 2

# Validate the find dialog is open
validate dialog_open=Find

screenshot e2e_find_replace

