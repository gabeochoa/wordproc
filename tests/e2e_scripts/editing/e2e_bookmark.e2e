# Test: Bookmark creation and navigation
# Validates bookmark creation via Insert menu

# Type some text on multiple lines
type "Section One"
key ENTER
type "Section Two"
key ENTER
type "Section Three"
wait 2

# Go to Section Two (line 1, 0-indexed)
key CTRL+HOME
key DOWN
wait 2

# Insert bookmark at current position
menu_open Insert
wait 2
menu_select "Bookmark..."
wait 2

# Validate bookmark was created (auto-named bookmark_2 since caret is at row 1)
validate has_bookmark=true
validate bookmark_count=1

screenshot e2e_bookmark

