# Test: Word selection using Shift+Ctrl+Arrow
# Validates that we can select words with keyboard

# Type some text with multiple words
type "One Two Three"
wait 2

# Use select all to verify selection works
key CTRL+A
wait 2

# Validate selection 
validate has_selection=true

screenshot e2e_double_click_word

