# Test: Italic expected but not applied
# This test validates that formatting validation catches missing italic
# Expected: FAIL (italic never applied, but validation expects it)

type "Test text"
wait_frames 12

# Select all but DON'T apply italic
select_all
wait_frames 6

# This validation should fail - we didn't apply italic
validate italic=true


