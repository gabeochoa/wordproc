# Word Count Targets & Progress Tracking

Set word count goals per session, per day, or per project. Track progress with visual indicators. Per-section word counts in the outliner. Writing streak tracking and statistics (words per day, total over time).

## Status

Word count display exists in the status bar. Missing: configurable targets, per-section counts, progress visualization, and statistics tracking.
