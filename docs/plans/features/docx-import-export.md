# DOCX Import & Export

Read and write Microsoft Word .docx files -- the most widely used document format. Preserves formatting, styles, tables, images, headers/footers, and track changes when round-tripping through .docx.

## Status

Not yet implemented. This is the most important missing format. The .docx format is XML-based inside a zip container and requires handling the OOXML specification.
