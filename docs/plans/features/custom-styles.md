# Custom Styles

User-defined paragraph and character styles beyond the built-in set. Create, edit, rename, and delete custom styles. Style inheritance and cascading so modifying a base style updates dependent styles automatically.

## Status

Built-in paragraph styles exist (title, subtitle, H1-H6, body). User-created custom styles, style editing, and inheritance are not yet implemented.
